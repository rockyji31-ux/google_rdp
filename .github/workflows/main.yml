name: Google RDP (Telegram Controlled)

on:
  workflow_dispatch:
    inputs:
      crd_code:
        description: 'Paste Google CRD Code Here'
        required: true

jobs:
  google-rdp:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: Setup Environment
        run: Write-Host "Telegram se Code mil gaya! Processing..."

      - name: Install & Configure CRD
        env:
          # Input ko environment variable me daal rahe hain (Safety ke liye)
          RAW_INPUT_CODE: ${{ github.event.inputs.crd_code }}
        run: |
          $rawCode = $env:RAW_INPUT_CODE
          
          if (-not $rawCode) {
            Write-Error "Error: Code nahi mila!"
            exit 1
          }

          # --- FIX: Naya Link (.msi) use kar rahe hain ---
          Write-Host "Downloading Chrome Remote Desktop..."
          Invoke-WebRequest https://dl.google.com/edgedl/chrome-remote-desktop/chromeremotedesktophost.msi -OutFile chromeremotedesktophost.msi
          
          Write-Host "Installing..."
          Start-Process msiexec.exe -ArgumentList '/i chromeremotedesktophost.msi /quiet /norestart' -Wait
          
          # --- PIN Fix Logic ---
          $finalCommand = $rawCode
          if ($finalCommand -notmatch '--pin=') {
              $finalCommand = $finalCommand + " --pin=123456"
          } else {
              $finalCommand = $finalCommand -replace '--pin=\d+', '--pin=123456'
          }
          
          Write-Host "Configuring CRD..."
          Invoke-Expression $finalCommand

      - name: Send Success to n8n
        run: |
          $WebhookUrl = "https://65.1.91.110.nip.io/webhook/rdp-credentials"
          $body = @{
              user = "Google User"
              password = "PIN: 123456"
              ip = "✅ Ready! Open: https://remotedesktop.google.com/access"
          } | ConvertTo-Json
          
          try {
             Invoke-RestMethod -Uri $WebhookUrl -Method Post -Body $body -ContentType "application/json"
          } catch {
             Write-Warning "Webhook Failed"
          }

      - name: Keep Alive
        run: while ($true) { Start-Sleep -Seconds 300 }
